// BeaconItem was the old carryable beacon: a runtime-built GrabbableObject / NetworkBehaviour.
// That custom-grabbable networking was fragile and crashed multiplayer clients, so the beacon was
// redesigned as a deploy-in-place object (see BeaconObject / BeaconFactory / BeaconManager). This
// file is intentionally empty (kept because the sandbox can't delete it).
